# QuickWeb CTF

This VM is a vulnerable Ubuntu Server intentionally created for educational purposes. The goal is to find the flag!

**Difficulty level:** Medium

## 📥 To download the VM:
```bash
wget https://github.com/rayanupc/QuickWeb_CTF/quickweb.ova
```

## ⚙️ Setup

The VM is compatible with **VirtualBox** or **VMware**. 

Double click on the `.ova` file and start the challenge.

## 🚩 You have to find the flag!

The flag is hidden deep within the system. You'll need to identify vulnerabilities, exploit them, and extract the flag.

## 🛠️ Tools Required

- `wpscan` - WordPress vulnerability scanner
- `searchsploit` - Exploit database search
- `curl` - HTTP tool
- `netcat` - Network utility
- `exiftool` - Metadata extraction

## ⚠️ Security Notice

This VM contains intentional vulnerabilities for **educational purposes only**.

- Do NOT use in production
- Do NOT deploy on public networks
- Use only in isolated learning environments

---

**Good luck! 🚩**
